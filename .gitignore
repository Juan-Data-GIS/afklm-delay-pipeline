# Environnements (secrets)
.env
.env.dev
.env.prod

# Alertmanager SMTP secret (Google App Password 16 chars, jamais versionner)
.smtp_password

# dbt
profiles.yml
.user.yml
.dbt/
logs/
# Garder le dossier target/ versionne via .gitkeep pour eviter les
# mounts root-owned au premier docker compose up sur un fresh clone
# (le mount ./target: a ete introduit dans PR #21).
target/*
!target/.gitkeep
dbt_packages/

# dlt
.dlt/secrets.toml

# Python
venv/
.venv/
__pycache__/
*.py[cod]
.DS_Store

# Notes d'avancement (locales)
avancement_*.md


# Compléments DLT
.dlt/.var
_local
*.duckdb
*.wal

# Snapshots et Backups locaux de la base de données
backup/